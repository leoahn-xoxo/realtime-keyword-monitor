# Render 무료 배포 설정 (https://render.com)
# GitHub 저장소를 연결하면 이 파일을 읽어 자동으로 웹 서비스를 생성합니다.
services:
  - type: web
    name: realtime-keyword-monitor
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
